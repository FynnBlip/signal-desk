# 发布文件清单 · 当前工作树

对象：`signal-desk/` 当前工作树，不是 git HEAD，也不是上一级父目录。
`data/` 实验产物、模型权重、`.env`、`.hallmark/` 本地设计截图不进入发布包。
本清单只规定“准备发布时应纳入审查的文件”；**在你明确授权之前，不会 commit 或 push。**

## 必须纳入（当前有些仍是未跟踪文件）

| 路径 | 原因 |
|---|---|
| `LICENSE` | MIT 许可 |
| `README.md` / `requirements.txt` / `.gitignore` / `.env.example` | 安装与密钥占位 |
| `design.md` / `tokens.css` / `tokens.json` / `启动.bat` | 设计依据与 Windows 启动 |
| `app/lab.html` `app/lab.js` `app/lab.css` | 当前主工作台；`/` 与 `/index.html` 实际返回 lab.html，三者缺一不可 |
| `app/index.html` | 保留的旧实现，不是当前服务首页 |
| `app/setup.html` | 首次准备指南 |
| `app/insights.css` `app/insights.js` `app/scenes.css` `app/workbench.css` `app/workbench.js` | 保留的旧实现依赖，不由当前 lab 壳加载 |
| `server/main.py` `server/contracts.py` `server/provider_registry.py` `server/run_store.py` | 后端；contracts/registry/run_store 目前可能仍未跟踪 |
| `modules/**` 七槽 `*.py` 与 `manifest.py`，含 `modules/04_channel/wiring.py` | 运行时实现 |
| `scripts/bootstrap_demo.py` `scripts/smoke_test.py` `scripts/check_release_tree.py` | 布线夹具、只读冒烟、发布树检查 |
| `tests/test_core.py` `tests/test_first_run.py` `tests/test_observability.py` `tests/test_lab_workflow.py` `tests/test_release_tree.py`、`tests/lab_workflow.test.cjs`、`tests/lab_acceptance.test.cjs`、`tests/test_workbench.cjs` | 当前回归；旧 insights/ui_audit 脚本仅作历史覆盖 |
| `docs/` 中的 ADR、验收、发布前检查、本清单、`asset_manifest.example.json` | 口径与首次使用 |
| `data/README.md` `models/README.md` `modules/README.md` | 边界说明；不含权重与录音 |

## 明确排除

- `.env`、任何填好的密钥
- `data/` 下除 README 外的全部本地产物（含噪声、矩阵、评分、Cohort）
- `models/**/*.pt|onnx|bin` 等权重
- `.hallmark/` 本地设计过程图
- `__pycache__/`、`.pytest_cache/`、虚拟环境
- 父目录里的历史归档或其它项目

## 本轮未做、不能当作已验收

- 独立 Python 3.12 虚拟环境完整安装 `requirements.txt`（含 torch），未记录下载量与耗时
- 无资产机器上的真实最小比较（AudioBox + 正式噪声 + 26 音色）
- GitHub remote、登录、push、远端干净克隆

检查命令（不提交）：

```text
python scripts/check_release_tree.py
python -m unittest discover -s tests -v
python -m compileall -q server modules scripts
```

# 2026-09-10 主流程迁移验收

## 当前入口
- `/`、`/index.html` 和 `/lab.html` 使用同一套 `lab.html` / `lab.css` / `lab.js`。
- 新版不加载 `workbench.css`、`insights.css`、旧 index 脚本，也不链接回旧布局。
- 旧源码保留作回退依据，未删除已有实验或 TTS 缓存。

## 已迁移
- 七模块导航、当前模块标记、全局运行状态。
- 当前固定探针；当前 TTS 缓存优先、其他缓存折叠。
- 实测声线分布、簇筛选、点选与缓存试听。
- 六场景统一色彩、噪声试听、固定信道参数说明。
- 版本选择、首次建立基准、就绪检查、启动与取消、实时 Trace。
- 当前实验六场景图；四版本基准作为独立来源，详情与刷新保持来源。
- 当前运行 A/B 试听、听感记录、Gate 依据、人工确认/放弃。
- 历史证据展开、报告、新建实验保留音频缓存。

## 数据隔离
- 当前轮次按 experiment_id 过滤，建立新基准后也不恢复上一实验结果。
- 空白实验不读取旧轮次或旧待确认结果。
- 未确认结果存在时禁止再次运行覆盖；确认前校验 run_id。
- 历史记录不能重新提交为本轮判断。

## 验证
- Python unittest 原 67 项通过；另增隔离工作流测试：基准→运行→听感记录→确认→新实验，真实状态与缓存未修改。
- `node tests/lab_workflow.test.cjs` 验证空白/跨实验隔离、就绪状态、防覆盖、过期确认、首次基准和无 TTS 调用。
- 浏览器逐页检查导航、历史展开、图表来源、刷新、空态与新建弹窗；声线缓存播放已验证。
- 1440px 与 390px 检查无横向溢出。

## 范围边界
本轮交付固定测试集的主工作流；任意语料编辑、重新生成 TTS 批次、独立模块自由参数运行不在此主流程中展开。
新增听审与确认接口使用隔离数据完成串联验证，未替用户提交真实听感或晋级结论，也未发起真实推理。

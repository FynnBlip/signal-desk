# GitHub 首次使用检查 · 2026-09-08，B/C 更新 2026-09-09

## 当前入口与验收命令（2026-09-16）

以下历史记录针对旧界面；当前首页统一为 `app/lab.html`，加载 lab.js/lab.css。首次资源准备已在当前 TTS / 声线页接回，并完成隔离模拟流程验收，见 `artifacts/codex-prepare-20260916/交付记录.md`；真实付费生成与评分仍需单独验收，不能凭历史记录宣布全链路通过。

```text
python scripts/check_release_tree.py
python -m unittest discover -s tests -v
node tests/lab_workflow.test.cjs
node tests/lab_acceptance.test.cjs
node tests/lab_incremental.test.cjs
node tests/lab_prepare.test.cjs
python scripts/smoke_test.py
```

最后一项要求服务已启动，只检查已有状态，不代表新执行了推理。2026-09-16 已用独立 Python 3.12 venv 从 `requirements.txt` 安装依赖，并在不复制 `.env`、模型、音频和实验状态的源码副本上完成启动、HTTP 与真实浏览器空白态检查；证据见 `artifacts/acceptance-next-20260916/验收续记.md`。2026-09-17 又实际验证了实验历史、场景摘要、156 条逐样本证据、实验报告和产品内返回路径；证据见 `artifacts/acceptance-next-20260917/验收续记.md`。这些检查仍不等于已验证付费 Provider、模型权重下载或真实推理。`tests/test_workbench.cjs` 用现有 Playwright 验证当前 lab 入口，仅允许 GET 并拦截写请求；需要本机已有 Playwright/浏览器，未安装时不能宣称端到端已运行。旧 `test_ui_audit.cjs`、`test_insights.cjs` 和标记 legacy 的 HTML 断言仅覆盖旧实现。

隔离模拟页面服务：`python tests/serve_lab_fixture.py --port 8091`。它只在内存处理明确列出的 API，不转发真实 Provider，不写实验数据。`GET /__test__/running` 使下一次结果轮询从模拟 running 变 done。可用 `SIGNAL_DESK_TEST_URL` 指定浏览器测试的目标；用完停止这个独立测试进程，不停止用户的 8090 服务。

候选包应不含密钥、模型和本机实验产物，单独启动后实际检查 `/`、`/lab.js`、`/lab.css` 的内容与页面初始化。文件检查、语法编译不能替代这一步；新安装与付费推理另行验收。

检查对象是当前工作区代码，不是尚未发布的 GitHub 仓库。已在临时目录复制 app/server/modules，排除 .env、模型和实验数据，使用 8091 端口验证首次打开。沿用本机 Python 依赖，因此不等于全新电脑安装验收。没有调用付费合成，也没有修改主工作区实验。

## 发布前阻断

更新：26 音色 / 四簇默认入口、生成前数量校验、四项准备清单、真实状态标题、页面内准备指南已实现。B 轮补充：正式噪声必须可解码；空文件/损坏 WAV 不能就绪；bootstrap 合成夹具按内容哈希识别，改名无效；未知来源可跑通但标记「来源未声明」；首页把「资产已检测到」和「真实推理已验证」分开。C 轮补充：工作树发布清单与 `scripts/check_release_tree.py`，语法级空拷贝编译。2026-09-16 已补独立 3.12 venv 安装与无资产真机最小启动；真实付费生成、模型权重安装和推理仍未执行。

下表保留审查时发现的问题。P0 前三项与准备清单已有修复；干净环境安装与 GitHub 发布仍未完成。文件名识别只针对本项目已知布线素材，内容哈希覆盖改名复制，不是通用的音频真实性或许可认证。

| 优先级 | 已确认问题 | 新用户影响 | 完成标准 |
|---|---|---|---|
| P0 | 声线数量只有 10/25/50/100，推荐 50；激活基线和 Loop 都要求恰好 26 个不同 voice_id、四簇 | 按推荐生成后不能锁为基线；可能先花费调用才发现不兼容 | 新手入口默认提供符合固定矩阵要求的配置，并在生成前校验；保留独立声线工具的其他数量 |
| P0 | 无资产首页承诺“跟着示范跑一轮”，实际跳入 MiniMax Cohort 配置 | 尚未体验价值就进入专业配置和外部账号准备 | 区分只读示例与真实实验；前者明确来源，后者明确前提、调用与产物 |
| P0 | bootstrap 合成噪声生成后，benchmark.readiness.noise 从 false 变为 true | 只检查 WAV 存在，未在就绪判定中区分布线素材与正式测试素材 | 合成素材可做布线检查，但不能被标成正式基准已就绪；产物保留来源 |
| P1 | 空工作区同时缺 Cohort、六场景噪声和评测模型，首页只呈现第一项；仍显示“测试条件已锁定” | 修好一项再撞下一项，状态文案误导 | 一次展示准备清单，每项有操作入口，并按真实状态显示未准备/已准备 |
| P1 | README 的快速开始没有完整覆盖从现有 WAV/噪声素材到固定 26 音色矩阵的流程 | 没有开发者本地资产就无法复现主流程 | 增加可执行的素材获取/放置/导入/校验指南与成功示例；第三方许可单独说明 |
| P1 | 仓库无 remote，前端资源、后端支持文件、测试、LICENSE 等仍有未跟踪文件 | 直接上传已有提交会漏掉当前运行依赖 | 审查发布文件清单后再纳入 Git；检查干净检出，最后由用户授权提交/推送 |

## 本次已修复（含 B/C）

- 切换主页面停止页面内音频和独立噪声预览。
- 原始评分表区分缺失值与真实零分，不再生成 0.000 / NaN 假占位。
- 场景最佳高亮附带版本，底部明确“综合推荐”，纵轴说明补充显示区间。
- 工具页主标题统一 28px / 36px。
- 场景详情标题与推荐单元格沿用统一场景 ink 色。
- 正式噪声：解码、时长、采样率校验；空文件不能就绪。
- 布线夹具按规范 WAV 字节哈希识别，改名复制仍拒绝。
- 未声明来源的可解码 WAV 可以就绪，但不宣称已认证。
- 首页区分资产检测与真实推理记录。
- 发布候选文件清单与密钥/权重扫描脚本；`data/` 仍 gitignore。

验证通过：`python -m unittest discover -s tests -v`（本轮 B/C 后应再跑）。`tests/test_ui_audit.cjs`、`tests/test_workbench.cjs`、`tests/test_insights.cjs` 需要已启动服务及已有比较数据，本轮未重跑。测试拦截 API 写请求。

## 尚未验证，不能据此宣布可发布

- 干净 Python 3.12 虚拟环境中的完整依赖安装、下载量和安装时间。本轮故意未执行：torch / audiobox 体积大，不能把本机已装环境当成新电脑验收。
- 无任何本地历史资产时，从准备到真实评分、人工确认、重启恢复的完整一轮。
- GitHub 登录、远程仓库权限、实际推送和远端干净克隆；当前仅确认未配置 remote。
- 浏览器端到端与真实 AudioBox/ONNX 推理。

建议顺序：用户授权后再把清单内未跟踪文件纳入 Git → 干净环境安装记录 → 有资产的最小真实比较 → 配置 GitHub 并发布。
